pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build is complete'
            }
        }
        stage('Test') {
            steps {
                echo 'Tests are running'
            }
        }
    }
}
